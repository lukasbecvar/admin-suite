<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="Admin suite linux servers and app managment dashboard" name="description">
    <link rel="icon" href={{ asset('assets/images/favicon.png') }} type="image/x-icon"/>
    {# INCLUDE PWA CONFIG MANIFEST #}
    {% if pwa_app_support == 'true' %}
        <link rel="manifest" href={{ asset('pwa-config.json') }}>
    {% endif %}
    {# REGISTER CSS ASSETS #}
    {{ encore_entry_link_tags('index-css') }}
    <title>Admin suite</title>
</head>
<body>
    {# MAIN COMPONENT WRAPPER #}
    {% block body %}{% endblock %}
    
    {# SCREEN TOO SMALL ERROR MESSAGE #}
    <div class="fixed inset-0 flex items-center justify-center bg-black/80 text-white hidden xs:flex short:flex p-4 z-50">
        <div class="bg-neutral-900 border border-neutral-700 rounded-xl shadow-2xl max-w-md w-full p-6 text-center space-y-4">
            <div class="flex justify-center">
                <i class="fas fa-exclamation-triangle text-yellow-400 text-4xl"></i>
            </div>
            <h2 class="text-xl font-semibold">Screen too small</h2>
            <p class="text-sm text-gray-300">
                Your screen width is too small to display this application properly. <br/>
                Please enlarge your window or use a larger device.
            </p>
        </div>
    </div>
</body>
</html>
