<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Suite Monitoring</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1e1e1e;
            color: #ffffff;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #2d2d2d;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            border: 1px solid #8b8b8b;
            border-radius: 8px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        h2 {
            position: relative;
            display: inline-block;
            padding-bottom: 10px;
            color: white;
            margin: 0;
        }
        h2::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: 0;
            width: 50%;
            height: 3px;
            background-color: #ff5555;
            transform: translateX(-50%);
        }
        .content {
            color: #ffffff;
            background-color: #333;
            padding: 15px;
            border-radius: 5px;
            margin-top: 20px;
            word-wrap: break-word;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 0.9em;
            color: #aaaaaa;
        }
        .footer p {
            margin: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>{{ subject|e }}</h2>
        </div>
        <div class="content">
            {{ message|e }}
        </div>
        <div class="footer">
            <p>&copy; {{ time|e }} Admin Suite. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
