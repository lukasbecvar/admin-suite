<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="content-type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Suite Monitoring</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1e1e1e;
            color: #ffffff;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            padding: 20px;
            background-color: #2d2d2d;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
            border: 1px solid #8b8b8b;
            border-radius: 8px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        h2 {
            position: relative;
            display: inline-block;
            padding-bottom: 10px;
            color: white;
            margin: 0;
        }
        h2::after {
            content: '';
            position: absolute;
            left: 50%;
            bottom: 0;
            width: 100%;
            height: 3px;
            background-color: #ff5555;
            transform: translateX(-50%);
        }
        .event-info {
            margin-top: 20px;
        }
        .info-item {
            margin-bottom: 10px;
            padding: 10px;
            background-color: #333;
            border-radius: 5px;
            color: #ffffff;
            display: flex;
            justify-content: space-between;
            font-weight: bold;
        }
        .info-item span {
            font-weight: bold;
            color: #ffa9a9;
        }
        .alert {
            margin-top: 20px;
            background-color: #3b3b3b;
            padding: 15px;
            border-left: 4px solid #fd4d4d;
            border-radius: 4px;
            color: #ffffff;
        }
        .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 0.9em;
            color: #aaaaaa;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h2>Monitoring Alert</h2>
        </div>
        <div class="event-info">
            <div class="info-item">
                <span>Service name:</span>
                <div>{{ serviceName|e }}</div>
            </div>
            <div class="info-item">
                <span>Alert:</span>
                <div>{{ monitoringMesssage|e }}</div>
            </div>
            <div class="info-item">
                <span>Time:</span>
                <div>{{ time|e }}</div>
            </div>
        </div>
        <div class="alert">
            <p>
                This email is automatically generated by Admin Suite. 
                It contains status updates and monitoring reports for your system.
            </p>
        </div>
        <div class="footer">
            <p>&copy; Admin Suite. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
