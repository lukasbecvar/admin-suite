{% extends 'common/layout.twig' %}

{# ACCOUNT SETTING CHANGE PASSWORD #}
{% block component %}
{# SUB NAVIGATION #}
<div class="bg-neutral-900 border-b border-neutral-700 sub-navigation text-white breadcrumb py-2 px-4 flex items-center">
    <a href={{ path('app_account_settings_table') }} class="border border-neutral-600 font-bold px-1 py-0 rounded mr-2 text-white" title="Back to account settings">
        <i class="fa fa-arrow-left" aria-hidden="true"></i>
    </a>
    <span class="mr-1">Password settings</span>
</div>

{# CHANGE PASSWORD FORM #}
<div class="flex justify-center items-center my-10 component">
    <div class="text-white max-w-md w-full bg-dark shadow-md px-3 py-1 border border-neutral-600 rounded m-w-95">
        <h2 class="text-xl mt-2 mb-2 text-center text-white">Change Password</h2>
    
        {# SUCCESS MESSAGE BOX #}
        {% for message in app.flashes('success') %}
            <div class="bg-green-700 text-white p-3 mb-3 rounded text-center">{{ message|e }}</div>
        {% endfor %}
    
        {# ERROR MESSAGE BOX #}
        {% for message in app.flashes('error') %}
            <div class="bg-red-700 text-white p-3 mb-3 rounded text-center">{{ message|e }}</div>
        {% endfor %}
    
        {# CHANGE PASSWORD FORM #}
        {{ form_start(passwordChangeForm) }}
    
            {# FORM FIELDS #}
            {{ form_row(passwordChangeForm.password.first, {'attr': {'class': 'auth-form-input', 'placeholder': 'Password'}}) }}
            {{ form_row(passwordChangeForm.password.second, {'attr': {'class': 'auth-form-input', 'placeholder': 'Confirm Password'}}) }}
    
            {# FORM SUBMIT BUTTON #}
            <button type="submit" class="mt-2 mb-2 bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline w-full">
                Change
            </button>
        {{ form_end(passwordChangeForm) }}
    </div>
</div>
{% endblock %}
