<div class="backdrop-blur-md bg-gray-800/50 border border-gray-700/50 rounded shadow-xl text-white w-full overflow-hidden">
	<div class="px-4 py-3 border-b border-gray-700/50 bg-gray-800/30">
		<h3 class="text-sm font-semibold text-white flex items-center">
			<i class="fas fa-terminal text-purple-400 mr-2"></i>
			SSH Access History
		</h3>
	</div>
	<div id="ssh-access-history-container" class="overflow-auto max-h-[320px] custom-scrollbar">
		{# LOADING SPINNER #}
		<div id="ssh-access-history-loading" class="p-3 space-y-2 text-center text-gray-400">
			<i class="fas fa-spinner fa-spin text-purple-400 text-3xl mb-2"></i>
			<div class="text-sm">Loading SSH access history...</div>
		</div>

		{# EMPTY STATE #}
		<div id="ssh-access-history-empty" class="hidden text-center text-gray-400 text-sm p-4">
			No SSH access history found
		</div>

		{# ERROR STATE #}
		<div id="ssh-access-history-error" class="hidden text-center text-red-400 text-sm p-4">
			Failed to load SSH access history
		</div>

		{# LOG LIST #}
		<div id="ssh-access-history-list" class="hidden space-y-1 p-1"></div>
	</div>
</div>

{# INCLUDE FUNCTIONALITY SCRIPT #}
{{ encore_entry_script_tags('ssh-access-history-card-js') }}
